[translation]
provider = "openai_compatible"
base_url = "http://127.0.0.1:1234/v1"
model = "hy-mt1.5-7b"
target_language = "简体中文"
timeout_seconds = 5.0
# 同时发送的翻译批次数；还需要 LLM 后端允许相应并发。
max_concurrency = 2
temperature = 0.7
top_p = 0.6
max_output_tokens = 2048
api_key_env = "GAME_SCREEN_TRANSLATOR_API_KEY"

[ocr]
language = "japan"
min_score = 0.60
cache_dir = ".cache/paddlex"
detection_model = "PP-OCRv6_small_det"
recognition_model = "PP-OCRv6_small_rec"
model_source = "bos"
# cpu 或 gpu:N；例如第二张显卡使用 gpu:1。
device = "cpu"
# 限制 PaddleOCR 并行线程，避免与游戏争抢全部 CPU 核心。
cpu_threads = 2
# 整屏输入会在检测阶段等比缩小，OCR 坐标仍会还原到原始画面。
detection_max_side = 1280
# OCR 后、稳定跟踪和 LLM 前过滤图标、数字及非源语言文字。
text_filter_enabled = true
# 保留英文文本；关闭后可以进一步排除英文桌面图标和游戏 UI。
translate_latin = true
# 日文与中文的纯汉字文本无法可靠区分，默认跳过；需要翻译「開始」等内容时开启。
translate_han_only = false

[preview]
blur_radius = 8.0
font_path = ""

[profiles]
# 每个游戏各占一个子目录，其中包含术语表和 SQLite 翻译缓存。
root_dir = "profiles"

[live]
# 相对于所选显示器左上角的捕获区域；width/height 为 0 表示延伸至显示器边缘。
left = 0
top = 0
width = 0
height = 0
monitor_index = 0
capture_fps = 15
change_poll_fps = 6
change_threshold = 3.0
stable_observations = 1
stable_ms = 0
clear_after_ms = 900
context_pairs = 8
max_batch_size = 8
capture_backend = "dxgi"
# 每次 OCR 完成后的额外休息时间；0 优先避免短字幕被跳过。
ocr_cooldown_ms = 0
